# Termux Spotify YouTube Downloader Requirements
# Install with: pip install -r termux_requirements.txt

# Core Spotify and YouTube libraries
spotipy>=2.22.1
yt-dlp>=2024.1.1

# Audio metadata and processing
mutagen>=1.46.0
pillow>=10.0.0

# HTTP requests and environment
requests>=2.31.0
python-dotenv>=1.0.0

# System monitoring and mobile optimization
psutil>=5.9.0

# Progress bars and terminal colors
tqdm>=4.65.0
colorama>=0.4.6

# Optional: Enhanced audio quality (if available)
# ffmpeg-python>=0.2.0  # Uncomment if you want Python FFmpeg bindings

# Note: FFmpeg itself is installed via Termux packages (pkg install ffmpeg)
# These are just the Python libraries needed for the maximum quality downloader